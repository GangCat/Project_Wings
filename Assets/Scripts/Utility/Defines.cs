public delegate void VoidBoolDelegate(bool _value);


public enum EPublisherType 
{ 
    NONE = -1, 
    GAME_MANAGER,
    LENGTH 
}

public enum EMessageType
{
    NONE = -1,
    PAUSE,
    LENGTH
}
